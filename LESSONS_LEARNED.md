# 量化与数据处理避坑指南 (Lessons Learned)

这份文档总结了在过去代码编写和模型迭代中犯过的关键错误。在后续开发新功能、处理数据拼接或升级图表时，必须首先查阅此文档，坚决杜绝同类错误的二次发生！

## 1. Pandas 数据拼接与时间轴对齐 (Fatal Error)
- **错误场景**：使用 pd.merge_asof() 进行多个异构数据源拼接时报错 incompatible merge keys [0] dtype('<M8[us]') and dtype('<M8[s]')。
- **根本原因**：Pandas 2.0+ 对 merge_asof 的数据类型校验极其严格。不同 API 传回的日期精度不同，且带时区与不带时区的混用，导致底层无法比较。
- **强制规范**：在合并任何时间序列前，**必须强制统一时间戳的精度和剥离时区**：
  df['date'] = pd.to_datetime(df['date']).dt.tz_localize(None).astype('datetime64[ns]')

## 2. 移动平均（Rolling）造成的前置数据截断 (Logical Bug)
- **错误场景**：在图表展示“过去 3 年”指标，但只拉取了 3 年数据，导致前 200 日因为 olling(200) 计算变 NaN 而丢弃，图表只剩 2 年。
- **强制规范**：计算 N 日滚动均线，数据拉取起点必须向前提早 N 个交易日（约增加1年），计算完后再按时间截取。

## 3. Python 字符串与 LaTeX 渲染的转义冲突 (UI Bug)
- **错误场景**：st.markdown("""  Z = \frac{x}{y}  """) 公式乱码。
- **根本原因**：Python 字符串将 \f 解析为换页符。
- **强制规范**：写 LaTeX 必须使用原始字符串前缀 """...""" 或 \\frac。

## 4. Pandas API 弃用崩溃 (Deprecation Error)
- **错误场景**：df.style.applymap() 报错。
- **强制规范**：Pandas 2.1.0 已废弃 applymap，全部改用 df.style.map()。

## 5. API 返回列名大小写敏感 (KeyError)
- **错误场景**：FRED 返回 date，Yahoo 返回 Date，直接执行 set_index('Date') 崩溃。
- **强制规范**：set_index 前必须做防御性重命名：if 'date' in df.columns: df = df.rename(columns={'date': 'Date'})。

## 6. 布尔逻辑判断与 np.nan 造成的隐式崩溃 (TypeError)
- **错误场景**：在评估回测胜率时，写了 sum(...) / len(tw_3m) if tw_3m and len(tw_3m) > 0 else np.nan。当没有信号时，函数返回了 np.nan。导致执行 len(np.nan) 直接宕机。
- **根本原因**：在 Python 中，bool(np.nan) 的值是 True！这导致 if tw_3m 判定通过，随后强行对一个 float 执行 len() 操作。
- **强制规范**：
  1. 如果下游逻辑期待一个列表，那么在无数据时绝对**不能返回 np.nan**，必须返回空列表 []。
  2. 永远不要使用 if variable: 来判断一个可能为 np.nan 的对象是否有效，应使用明确的类型检测 isinstance(var, list) 或 pd.isna(var)。

## 7. 外部 API 滥用与本地主数据缺失 (Local-First Failure)
- **错误场景**：在已经拉取并整理好本地数据后，脚本和测试代码依然残留调用 yf.download()，导致遇到网络限流、超时卡死半小时，并吐出海量进度条废话日志，严重浪费 Context Token。
- **根本原因**：编写代码时存在惰性，直接复制了带网络请求的代码模板，没有践行“本地优先（Local-First）”原则。
- **强制规范**：
  1. 必须建立标准统一的本地主数据集（如 market_data_local.csv），后续所有回测、绘图与计算**一律强制 100% 读本地文件**。
  2. 严禁在计算脚本中裸调外部网络 API！

## 8. 盲目自我感动与牛市现金拖累陷阱 (Complacency & Cash Drag Trap)
- **错误场景**：在长达 17 年的超级牛市中，策略因为在黄昏期持有 25% 现金、去杠杆期持有 65% 现金，导致总收益被腰斩（+308% 跌至 +144%），却盲目吹嘘“回撤砍半、极其亮眼”。
- **根本原因**：只看风险指标，忽视了长牛市中现金机会成本（Cash Drag）的毁灭性打击，严重脱离权益投资的核心目标（财富积累与超额 Alpha）。
- **强制规范**：
  1. 必须时刻对真实最终资产负责，严禁将总回报大幅跑输 Buy & Hold 的策略作为最终成果交付！
  2. 在正常牛市扩张期必须保持 100% 满仓，唯有在极端系统性泡沫破裂和流动性危机时才可战术避险，并在恐慌极值深度抄底以获取真正超额收益。

## 9. Matplotlib 绘制含 '$' 符号与中文时的 LaTeX 转义与字体冲突 (UI Glyph Bug)
- **错误场景**：在 Matplotlib 图表标题或标签中写入 $ 符号（如 $192,000）且包含中文，终端报错 Font 'rm' does not have a glyph for [U+XXXX] 并产生方块乱码。
- **根本原因**：Matplotlib 将 $ 符号自动解析为 LaTeX Math Mode 入口，而 LaTeX 默认字体（'rm'）不包含中文字符集。
- **强制规范**：
  1. 在含有中文的标题和坐标轴文本中，严禁直接使用裸 $ 符号，应写作 USD 192,000 或使用转义 \\$。
  2. 绘图脚本必须显式设置：
     plt.rcParams['font.sans-serif'] = ['SimHei', 'Microsoft YaHei', 'SimSun', 'sans-serif']
     plt.rcParams['axes.unicode_minus'] = False

## 10. 虚假收益率连乘与真实券商记账脱节 (Phantom Compounding Bug)
- **错误场景**：使用 strat_balance *= (1 + daily_return) 简单模拟仓位，忽略了空仓买回时的股价高低和持股股数稀释，得出虚假的“跑赢基准”假象。
- **强制规范**：
  必须使用券商级两状态变量记账：strat_shares（实际持股数）与 strat_cash（流动现金池）。每一笔交易均按成交价严格折算，真实反映股数增减。

## 11. 拒绝特例修补与后视镜过度拟合 (Overfitting & Curve-Fitting Trap)
- **错误场景**：在扩展多资产时，为了让软件板块（IGV）不踏空 2020 年因新冠居家红利带来的特殊 V 型超跌反弹，私自引入了 `trend_reclaimed`（右侧双均线收复买入）补丁。结果该补丁渗透到主模型，在 2022 年 3 月熊市假反弹中诱发 QQQ 误买被套，导致 QQQ 收益率从 +967.0% 暴跌至 +883.7%（损失近 18 万美元超额收益）。
- **根本原因**：典型的“后视镜拟合/刻舟求剑”。为了迎合单一行情个案的数据好看，破坏了“宏观锚未修复前严禁进场”的第一性原理。过度拟合的补丁必然会在真实未来的样本外或其他资产中遭到残酷惩罚。
- **强制规范**：
  1. 坚决禁止为了追求回测数字完美而发明 ad-hoc 特例补丁！宏观底层状态机必须保持逻辑自洽与一致性。
  2. 坦然接受不同资产在特定历史极端阶段的宏观摩擦与回撤，如实汇报客观结果，严禁通过修改交易规则进行数据造假与自我欺骗。

## 12. 多维度雷达打分与决策层级原则 (Radar Scaling & Layer Primacy)
- **错误场景**：在构建行业微观泡沫雷达时，试图直接用硬编码常数线性相加各指标（如 LPPLS、PS估值偏离度、广度），导致基础均值失衡，且强行用离散买卖点做频繁回测反而产生均线缠绕磨损。
- **根本原因**：
  1. 各分项因子的量纲、均值和极端分布完全不同，简单线性相加会被高方差项劫持。
  2. 机械化的均线破位买卖点对于牛市高位震荡过于敏感。相比之下，**Layer 2 (0~100 综合与 4 维度独立雷达看板)** 提供的连续风险感知，以及 **Layer 3 (权重股内部广度与顶背离)** 揭示的主力撤退“指数掩护”真相，其实际指导意义与决策价值远高于孤立的机械买卖点。
- **强制规范**：
  1. 必须对各分项指标执行动态扩展窗口的分位数归一化（Rolling Dynamic Percentile），将不同量纲映射为均值 50、范围 0~100 的无量纲统一雷达分。
  2. 在投研展示端（Streamlit/看板）与 GitHub 交付物中，**重点突出 Layer 2 连续雷达预警与 Layer 3 内部广度顶背离**，使投研人员直观洞察行业健康度与泡沫积聚程度。

## 13. 信号信噪比失真与状态门禁缺失 (Signal-to-Noise Ratio & Regime Gating Trap)
- **错误场景**：将“右侧突破 50MA”简单作为 OR 分支直接纳入“客观抄底信号”，导致在 2025 年初超级大牛市历史大顶（股价 $225，较年线高出 +29%）由于微调后站上 50MA 而荒谬打出“蓝色抄底标志”；同时因缺乏相空间迟滞滤波，牛市中股价微调 2 天跌破 10MA 就报“黄色逃顶”，反抽站上 50MA 又报“蓝色抄底”，导致主升浪中蓝黄标志密集交替闪烁，完全沦为均线缠绕噪音。
- **根本原因**：
  1. **概念偷换与逻辑混淆**：混淆了“右侧顺势趋势突破”与“逆势极度恐慌抄底”，把牛市高位横盘突破当成抄底。
  2. **状态机门禁缺失（Regime Gating 缺失）**：未设置物理相空间的硬性门禁。真正的“抄底”第一性原理必须发生于负势能超跌区（`Dist_200MA < 0` 且多处于 -10% 以下）；真正的“逃顶”必须发生于正势能泡沫扩张区（`Dist_200MA > 10%` 且综合得分处于极高位）。
  3. **缺乏迟滞滤波与状态冷却（Hysteresis & Cooldown 缺失）**：相空间相变是宏观波段的跃迁，微观日线的一两天杂波不应反复触发相变警报。
- **强制规范**：
  1. **绝对门禁准则**：严禁在年线上方追高打出任何“超卖抄底”标志；严禁在熊市腰斩深渊打出“高位泡沫逃顶”标志！
  2. **动能拐点唯一性**：仅在速度导数初次发生由负转正（$\dot{q}_1 > 0$）或由正转负（$\dot{q}_1 < -0.3$）的破位拐点当天打点，严禁日线级别连续缠绕打点。
  3. **波段迟滞去噪**：设置波段冷却期（18~25个交易日），同向信号在未创出更极端极值价格前严禁重复弹窗，确保雷达信号具备极高的信噪比与宏观战略指导价值。

## 14. 粗暴硬阈值一刀切导致的“牛市中枢考验买点抹杀”陷阱 (Ad-hoc Hard-Gate & Stage Bottom Suppression Trap)
- **错误场景**：在修复 2025 年初 $225 历史大顶（年线偏离 +29%）被误打出“抄底”信号的 Bug 时，为了图省事，直接机械地硬塞了一条补丁 `gate_bottom = Dist_200MA <= -2.0%`，并把所有底部都归为“崩盘恐慌超跌”。结果导致在长达 13 年的牛市中，所有在 50MA 或 200MA 上方悬停回踩（偏离度在 $0\% \sim +6\%$）的健康洗盘、中枢回踩蓄势买点（如 2014-2015、2018-2019、2024年中等阶段大底），被一网打尽、全部归零，图谱上阶段底部毫无任何建仓/加仓信号！
- **根本原因**：
  1. **刻舟求剑式补丁**：根据单个极端异常值（$225 偏离 +29%）拍脑袋制定全局静态阈值，没有深究经济学机制。
  2. **混淆了两种本质不同的底部物理形态**：
     - **极度恐慌崩盘底 (Type A: Panic Crash Reversal)**：熊市自由落体、流动性危机、远离均线的极度负偏离 V 型反转；
     - **牛市阶段蓄势底 (Type B: Stage Consolidation & Equilibrium Test)**：索罗斯繁荣-萧条序列中的**【考验期 (Period of Testing)】**。在长牛结构中回踩均线中枢生命线，过热能量完全冷却，李雅普诺夫吸引子验证有效，相空间动能由负转正！
  3. 用硬性 `Dist <= -2%` 压制，相当于认为“只有跌破年线才配叫底部”，彻底抹杀了牛市强趋势中“不破年线即重拾升势”的经典加仓契机。
- **强制规范**：
  1. **严禁使用单一粗暴门禁兼并所有交易场景**！必须建立严格对应反身性宏观与相空间机理的**三层独立信号体系**：
     - **【类型一：反身性极度恐慌底 (Type A)】**：针对熊市暴跌极值区，必须折价 ($Dist\_200MA \le 0\% \lor close < MA50$) 且动能转正；
     - **【类型二：反身性牛市阶段蓄势底 (Type B)】**：针对牛市考验期，结构健康 ($MA200\_Slope \ge 0, MA50 \ge MA200$)，中枢回踩 ($Dist\_200MA \in [-12\%, +8\%]$ 或回踩 $MA50$)，过热宣泄 ($Score \le 60$) 且动能转正；
     - **【类型三：反身性极度泡沫顶 (Type C)】**：针对繁荣极峰相变，过热极端且位于高位，动能破位衰竭。
  2. **图谱可视化必须分层标示**：用不同形状/色彩明确区分“极度恐慌底”与“阶段蓄势底”，严禁概念混淆与一刀切误导！


## 15. 成交时点前视与隔夜跳空代价陷阱 (Execution Timing & Future Leak Trap)
- **错误场景**：过去模型在生成 T 日收盘信号时，直接假设能够以 T 日的收盘价成交。
- **根本原因**：严重的“未来函数 (Look-ahead Bias)”。在真实交易中，当 T 日收盘并确立信号时，市场已经闭市，绝不可能“穿越”回去用当天的收盘价成交。这种前视偏差完全抹杀了隔夜跳空 (Overnight Gap) 与次日滑点 (Slippage) 的巨大摩擦成本，导致回测收益率呈现严重的虚高幻觉。
- **强制规范**：
  1. 必须在引擎底层引入严密的延迟执行状态机。
  2. 对于具备完整开高低收 (OHLC) 数据的资产，强制采用 `NEXT_OPEN`（T+1 日开盘）成交。
  3. 对于仅具备单列收盘价数据的资产，强制采用 `NEXT_CLOSE`（T+1 日收盘）成交，并严格遵循“先承担价格波动 -> 估值 -> 入金 -> 成交新订单”的时序闭环。

## 16. 缺乏全局闭环与阶段性实证验证的执行陷阱 (End-to-End Verification Failure)
- **错误场景**：在修复图表重叠或模型 Bug 时，仅仅聚焦于“修改了几行代码”或“调整了某个参数”，大脑里推演了一遍“理论上不会出错”或看到终端“没报 KeyError”，就直接汇报任务完成。结果交付后依然出现 UI 重叠或逻辑断层。
- **根本原因**：**陷入细枝末节，丧失了全局工程原则**。把“局部代码修改”等同于“全局结果达成”。没有建立“修改 -> 阶段性端到端实证 -> 确认符合最终要求 -> 交付”的完整闭环。
- **强制规范**：
  1. **摒弃“盲人摸象”式的局部修复**：完成任何一个阶段的任务后，必须跳出代码细节，以最高视角去检视**最终产出物（如 HTML渲染树、真实的交易记录对比）**是否符合用户的真实诉求。
  2. **没有看到真实结果 = 没有完成**：无论是排版布局还是金融逻辑，严禁用“理论推导”或“没有报错”来替代“实证实测”。哪怕搭建测试环境再繁琐，也必须穷尽一切手段（如导出离线文件、对比关键数据）完成实证闭环。

## 17. 上游异常引发的误导性报错、缓存毒化与一票否决级联崩溃陷阱 (Upstream Outage Misdiagnosis, Cache Poisoning & Cascading Failure Trap)
- **错误场景**：生产环境（Streamlit Cloud）Tab 4 / Tab 2 搜索美股代码（如 `NVDA`）时，界面直接报红字 `未找到标的 NVDA 的信息，请检查美股代码是否输入正确。`，且长达 30 分钟 ~ 6 小时无法恢复；用户刷新页面反而使上游封禁加剧。
- **根本原因**：
  1. **武断误报与甩锅用户**：上游（Yahoo Finance）在云端 IP 出现 401 封锁、429 限流或返回空数据/缺少身份字段时，前端粗暴将其误判为“美股代码输入错误”，将外部基础设施的暂时性故障误导为用户操作失误。
  2. **毒化长效缓存（Cache Poisoning）**：Streamlit 缓存函数在失败时内部捕获并直接 `return {}` 或 `return {"status": "error"}`。Streamlit 视其为合法的成功计算结果并缓存 30 分钟 ~ 6 小时，导致秒级网络抖动被固化为长达数小时的应用假死。
  3. **模块强耦合“一票否决”与缺乏本地脱机兜底**：公司画像、实时行情、财务三张表、新闻强行捆绑在单一获取链路中。只要公司资料失败，页面直接提前 `return`，连累本已拉取成功的行情、财报和本地已有的高频历史序列全部无法展示；财务报表内部也是一张表（如利润表）为空就整表抛弃。
  4. **高频重试引发雪崩效应（Retry Storm）**：缺乏状态机与冷却机制，在 401/429 发生后，页面每次自动重跑或用户手动刷新均发起真实网络请求，进一步加剧上游对宿主 IP 的封禁。
- **强制规范**：
  1. **受控异常防缓存毒化**：Streamlit `@st.cache_data` 装饰的底层抓取函数在遇到失败时**必须向上抛出受控异常（如 `StockDataFetchError`）**，绝不能在内部静默捕获并返回空字典，确保失败结果绝对不入长效缓存。
  2. **内存阶梯退避状态机（Cooldown Manager）**：建立跨线程的全局单例冷却管理器。遭遇 401 时触发 300 秒（5分钟）阶梯保护冷却，遭遇 429 遵循指数退避（60s -> 120s -> 240s）；冷却期内拦截上游物理调用并秒级返回冷却诊断，冷却到期后自动恢复探测，探测成功后立即清除冷却记录。
  3. **数据领域解耦与三大报表独立提取**：公司画像、行情、财报、新闻必须分别获取、分别缓存、分别记录状态。三大报表合并可用日期列，单表缺失绝不阻断其他表渲染；真实 0 负债如实保留为 `$0.00 B`，严禁误判为 `N/A` 缺失。
  4. **客观诊断，杜绝武断误导**：对用户一律客观展示“公司资料暂不可用（已启用保护机制）”，并提供诊断折叠区记录真实 HTTP 状态码与耗时，严禁下结论“代码输入错误”。
  5. **本地主数据集兜底且严守真实性红线**：优先支持从本地已清洗主数据集（如 `smh_constituents_local.csv`，截至 2026-09-11）提取真实收盘价序列兜底。严守真实性红线：仅有收盘价就只画折线图，绝不伪造 OHLC K 线，不冠以“实时”标签欺骗用户；模型推算值必须显式标注。

## 18. Pandas 时间索引隐式丢失与多源列缺项导致的运行时 KeyError 陷阱 (DatetimeIndex Demotion & Column Key Collapse Trap)
- **错误场景**：在 Tab 4 点击展开“3. 索罗斯反身性相空间动力学微观雷达”时，界面报错 `反身性雷达引擎计算异常: 'date'`。而在离线本地降级状态下，又潜伏着 `KeyError: 'high'`。
- **根本原因**：
  1. **时间索引（Index）与普通列（Columns）混淆**：yfinance `ticker.history()` 返回的 DataFrame 中，交易日期默认存储在 `pd.DatetimeIndex` 索引中（index.name 通常为 'Date'），而非普通列。上层适配代码在做列名小写规整 `df.columns = [c.lower() for c in df.columns]` 时，**未先调用 `reset_index()` 将时间索引提升为普通列**。导致转换后 DataFrame 只有 `['open', 'high', 'low', 'close', 'volume']`，`date` 列完全不存在，下游引擎直接调用 `df_price['date']` 抛出 `KeyError: 'date'`。
  2. **多数据源输入格式异构与防御性兜底缺失**：在线行情具备完整的 `['open', 'high', 'low', 'close', 'volume']`，但本地离线兜底主数据集（如行业成分股主数据）为真实历史收盘价序列，仅具备 `Date` 和 `Close`。数学引擎在计算资金流向（CMF20）等指标时，直接无防御访问 `df['high']`、`df['low']`、`df['volume']`，导致一旦切入本地离线降级，引擎立即崩溃为 `KeyError: 'high'`。
- **强制规范**：
  1. **时间日期列双向防御与自适应提升**：核心数学引擎与上层数据适配器**必须做双向防御**：
     - 首先检查 `date` 是否在普通列中；
     - 若不在且 `isinstance(df.index, pd.DatetimeIndex)`，必须无条件执行 `reset_index()`，并确保重命名为标准小写 `date`；
     - 剥离时区：`pd.to_datetime(df['date']).dt.tz_localize(None)`。
  2. **行情关键列自适应兼容降级**：
     - 数学引擎在接入数据时，必须显式做字段防御：若仅提供 `close`（如离线收盘价序列），`open`、`high`、`low` 必须自动以 `close` 兜底，`volume` 默认为 `0.0`，杜绝因单列缺失引发引擎级崩溃。
  3. **同类问题全局一次性斩草除根（严禁“打地鼠”）**：排查一处 `KeyError` 时，必须通盘梳理该函数在所有调用路径（在线完整数据、本地残缺数据、MultiIndex、无名索引）下的输入矩阵，确保任意路径均具备自动化单元测试（Exit Code 0）覆盖后再行交付。

