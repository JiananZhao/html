# AI 智能体开发规范与核心约束 (Agent Rules)

在进行任何代码开发、重构、Bug 修复之前，**必须绝对遵守以下规范**：

## 1. 强制学习历史教训 (Mandatory Lesson Review)
- 在执行任何编码任务前，**必须首先查阅**本目录下的 `LESSONS_LEARNED.md` 文件。
- 该文件记录了本项目中所有发生过的惨痛教训（如 Pandas 类型推断、布尔陷阱、LaTeX 转义等）。
- 在构思实现方案时，必须将当前方案与历史教训进行比对，**坚决杜绝同类错误的二次发生**。如果你重蹈覆辙，将被视为极其严重的失职。

## 2. 稳健第一 (Robustness First)
- 在处理外部网络请求（如 FRED, Yahoo Finance 等 API）时，绝不允许裸奔。必须加上重试机制（Exponential Backoff）和合理的超时设置。
- 在处理 Pandas 的异构数据源拼接时，绝不能依赖默认类型推断，必须进行严谨的数据类型强转和清理。

## 3. 防御性编程 (Defensive Programming)
- 任何布尔判断必须极其严谨。特别警惕 `np.nan`、`None` 和空数据框 `df.empty`。
- 在获取外部数据字段或计算列之前，必须检测键名是否存在。