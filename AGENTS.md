# AI 智能体开发规范与核心约束 (Agent Rules)

在进行任何代码开发、重构、Bug 修复之前，**必须绝对遵守以下规范**：

---

## 1. 强制学习历史教训 (Mandatory Lesson Review)
- 在执行任何编码任务前，**必须首先查阅**本目录下的 `LESSONS_LEARNED.md` 文件。
- 该文件记录了本项目中所有发生过的惨痛教训（如 Pandas 类型推断、布尔陷阱、LaTeX 转义、外部 API 滥用、虚假回测等）。
- 在构思实现方案时，必须将当前方案与历史教训进行比对，**坚决杜绝同类错误的二次发生**。如果你重蹈覆辙，将被视为极其严重的失职。

---

## 2. 纯本地优先与数据脱机闭环 (Local-First Architecture)
- **严禁在本地已有清洗数据的情况下，依然在脚本中残留调用 Yahoo Finance 或 FRED 等外部网络 API！**
- 一旦外部宏观与行情数据拉取并整理完毕，**必须立即永久固化为本地标准主数据集（如 `market_data_local.csv`）**。
- 后续所有模型回测、沙盘推演、绘图、Excel 导出以及应用加载，**必须 100% 强制从本地读取**。
- **严禁为了偷懒直接复制带网络请求的旧代码**：外部 API 存在不可控的超时、限流、字段变动风险，且网络日志和报错堆栈会严重污染 Context Window、白白烧毁海量 Token。
- 只有在明确需要增量更新历史数据时，才允许走受控的数据更新脚本。

---

## 3. 拒绝盲目自我感动，对最终投资财富负责 (Zero Blind Complacency)
- **严禁在策略总回报率严重跑输 Buy & Hold 时，仅仅因为“回撤变小了”或“夏普微涨”，就盲目吹嘘策略“表现极其亮眼”！** 这是严重的自我欺骗与对量化常识的侮辱。
- 做多权益类资产的核心目标是**实现财富增值与超额 Alpha**。在牛市中长期持有 25%~65% 现金导致总回报腰斩的策略，在真实商业世界中是不及格的废品。
- 在交付任何回测成果前，必须自发且主动地核验：
  1. 真实最终资产是否战胜了基准（Alpha > 0）？
  2. 是否存在长期空仓踏空主升浪的致命缺陷？
  3. 每一笔交易是否真正做到了“低买高卖”或“有效止损”？
- **严禁把明显存在致命逻辑缺陷的半成品当作最终成果汇报给用户！**

---

## 4. 主动闭环优化，杜绝“一问都知道，一做都不会” (Proactive Execution & High Agency)
- **严禁被动应付！** 凡是自己明明知道的优化项和最佳实践（例如：本地优先缓存、真实券商记账、防均线缠绕震荡滤波、Matplotlib 中文字体配置等），**必须在第一轮执行时自发、主动且彻底地落实到位**。
- 严禁出现“用户不质问就不改、一被质问才找借口补救”的恶劣作风。
- 在写代码和运行前，必须自检自查：
  1. 这段脚本是否还在裸调外部网络？
  2. 绘图代码是否配置了中文字体，标题中是否包含被 Matplotlib 误判为 LaTeX 的 `$` 符号？
  3. 记账体系是否追踪了真实股数与现金？是否存在幽灵复利假记账？

---

## 5. 真实券商记账体系强制规范 (True Brokerage Accounting Standard)
- **严禁使用任何形式的“每日收益率连乘（Phantom Compounding）”虚假记账！**
- 必须严格追踪账户的两个真实状态变量：
  * `strat_shares`：真实持仓股数；
  * `strat_cash`：真实闲置现金池。
- 卖出时：`strat_cash += strat_shares * price; strat_shares = 0`；
- 买入时：`strat_shares += strat_cash / price; strat_cash = 0`；
- 定投时：当月定投资金按当期仓位折算为新增股数与现金。
- 必须导出每笔波段的买卖价格对比与持股数量变化，严格杜绝频繁追涨杀跌磨损。

---

## 6. 深度批判性思维 (Critical Thinking & Step-by-Step)
- 在执行任何复杂的量化建模、指标构建或交易逻辑任务时，**必须执行 Step-by-Step 的推演**。
- **严禁生搬硬套或“一刀切”**（例如将所有资产视为同等相关性，或对高成长科技股生套保守宏观指标）。
- 必须时刻质问自己：这个逻辑在极端情况下（如暴跌、暴涨、恐慌）是否会导致荒谬的结果？例如：如果发出卖出信号，买入条件在现实中是否会导致“买入价比卖出价还高 20%”？
- 在写代码之前，先在沙盘里把各种极端边界条件推演一遍，绝不能只盯着表面的统计胜率而违背最基本的交易常识。

---

## 7. 严谨讲证据理论支持 (Theory & Empirical Evidence)
- 在执行任何复杂的量化建模、指标构建或交易逻辑任务时，**必须引用经济学或金融学理论，并给出量化证据来支持**。
- 严禁空穴来风，严禁拍脑袋决策。必须时刻质问自己：这个逻辑在现实中有理论依据？是否有量化证据支持？

---

## 8. 代码风格与可交付物规范
- 所有代码必须符合 PEP 8 规范，且有详细的注释和逻辑分析。
- 所有的模型交付物，必须包含：
  1. 完整可复核的 Excel 对账全底稿（含总表、逐笔买卖配对表、逐日流水表）；
  2. 3 层对齐的高清信号图谱（价格与买卖点标记、宏观反身性偏离度 Gap、真实券商净值对比曲线）。
