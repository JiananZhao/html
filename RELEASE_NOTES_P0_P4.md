# 量化增量引擎与真实记账重构 (P0-P4) 核心升级发布说明

本文档系统性总结了本项目从 **P0 到 P4** 阶段所经历的底层架构重构、业务逻辑纠偏以及 CI/CD 与前端防呆加固的所有核心成果。这标志着模型从“实验性后视镜脚本”正式蜕变为“可实盘复现的工业级增量引擎”。

---

## 🚀 P0 & P1：底层记账重构与执行器剥离 (Architecture & Execution)

### 1. 彻底消灭“虚假连乘复利 (Phantom Compounding)”
- **痛点**：旧版回测中通过 `balance *= (1 + return)` 模拟资产变化，这完全无视了真实世界的空仓滑点、持股股数稀释和现金拖累（Cash Drag），导致收益率存在严重的高估幻觉。
- **升级方案**：引入全新的券商级真实记账模块 **`true_accounting.py` (`UnitizedAccount`)**。
- **成果**：强制所有回测必须追踪 `strat_shares`（真实持股数量）和 `strat_cash`（真实流动现金池）双状态变量。所有的买卖点和资金调拨都必须经过严谨的资产重估与份额计算。

### 2. 消灭“未来函数 (Look-ahead Bias)”与执行时点规范
- **痛点**：旧逻辑在 T 日收盘计算出信号后，默认能以 T 日收盘价成交，这在物理世界中是绝不可能发生的。
- **升级方案**：重构并解耦出 **`shared_executor.py` (`SharedExecutor`)**，严格定义了时序隔离机制。
- **成果**：
  - 针对具备 OHLC 数据的资产（如 SPY, QQQ），强制采用 `NEXT_OPEN`（T+1 日开盘价）执行订单。
  - 针对单列收盘价数据（如部分微观雷达），采用 `NEXT_CLOSE`（T+1 日收盘价）执行订单。
  - 确立了 **“先承担隔夜价格波动 -> 重新估值 -> 入金处理 -> 结算新订单”** 的绝对时序闭环。

---

## 🛠️ P2：快照状态机与 O(1) 增量运行引擎 (Incremental Engine)

### 3. 特征生成与执行流的解耦
- **痛点**：每次更新数据都要将 14.5 年的历史长序列从头到尾进行一遍 O(N) 的特征计算与交易重放，不仅耗时巨大，而且随着数据拉长极易产生 OOM (内存溢出) 和 GitHub Actions 超时。
- **升级方案**：在各雷达模型（如 `energy_bubble_radar.py`）中，剥离“特征处理 (Feature Engineering)”与“执行重放 (Execution)”。
- **成果**：复杂特征（均线、偏离度、相空间导数）仍基于全量历史计算保证精度；但重放引擎改为增量执行。

### 4. 引入基于快照的 `StateManager`
- **升级方案**：构建了 **`core_engine/state_manager.py`** 状态管理模块。
- **成果**：
  - 支持将 `UnitizedAccount` 和 `SharedExecutor` 的关键内存状态序列化为 JSON 标量（如 `latest.json`, `ckpt_YYYY-MM-DD.json`）。
  - 支持 CSV 历史清单（`orders`, `fills`, `daily`）的局部读取与增量追加合并（O(1) 更新）。
  - 执行器每天只消费“相比于上次快照新增的、未处理过的新鲜交易日”，实现了真正的按日增量跑批。

---

## 🛡️ P3：缺价断路器、前端验证与元数据独立 (Robustness & UI Freshness)

### 5. 极端场景下的定向缺价与暂估逻辑 (Missing Price Resilience)
- **痛点**：Yahoo Finance 经常出现数据缺失，或者半天交易日无收盘价，导致简单的历史拉取引发 `NaN` 并导致整个净值计算崩溃。
- **升级方案**：在 `SharedExecutor` 中引入 `DELAYED_DUE_TO_MISSING_PRICE` 状态枚举。
- **成果**：
  - **订单与定投挂起**：当目标成交日（T+1）出现缺价时，暂缓执行买卖，资金标记为 `pending_cash`，直到价格恢复日才完成转账闭环。
  - **暂估净值修复**：在缺价日使用最新的实际持股数和上一交易日的有效价格进行暂估，防止净值（Equity/NAV）暴跌归零。

### 6. 前端动态新鲜度验证 (UI Freshness Guardian)
- **痛点**：用户打开 Streamlit 看板时，无法直观判断看到的数据是昨天的还是上周的陈旧缓存。
- **升级方案**：在 `industry_bubble_tab.py` 顶部引入基于 `yfinance` 的实际美股日历比对功能。
- **成果**：智能判别当前数据是否落后于最新的物理交易日。若落后，则在看板右上角醒目标红 `⚠️ 数据陈旧`；反之则显示 `⚡ 最新数据`。

### 7. 数据导出纠偏与 `manifest.json` 独立清单
- **成果**：
  - **配对表纠偏**：`export_utils.py` 生成回测买卖配对表时，全面改写逻辑，强制使用 `fills.csv` 记录的“真实成交价格”进行计算，不再回查历史 K 线提取理想价格。
  - **抛弃 CSV 污染**：抛弃了向 CSV 末尾强塞 Metadata 以致于破坏数据清洗管道的陋习，改为每次跑批输出独立的结构化发布清单 `manifest.json`。

---

## 🤖 P4：CI 持久化与隔离测试加固 (CI/CD & Validation)

### 8. Github Actions 云端原子发布
- **痛点**：过往的 CI 配置 (`data_updater.yml`) 中大量滥用 `|| true`，导致部分脚本崩溃或数据脏乱时，仍被视作“执行成功”并错误推送到 Master，造成线上污染。
- **升级方案**：
  - 坚决移除所有的 `|| true`，任何脚本的非 0 退出代码都会直接阻断 CI 流程，及时暴露问题。
  - 引入了 `actions/cache@v4` 来管理和保存 `.states/` 快照目录。
- **成果**：保证云端每日重运行时能准确抓取上一次的状态断点，使得“增量引擎”在无状态云环境（GitHub Ubuntu Runners）中成为可能。

### 9. 自动化隔离测试体系建设
- **成果**：新建了 `test_incremental_engine.py` 测试桩，脱离线上庞大数据，构造了包含 `[100.0, np.nan, 105.0]` 这种致命缺价的数据孤岛，成功在纯净环境下证明了状态机的原子写入、缺价挂起与断点重启功能的绝对可靠性。

---
**Summary**: P0 到 P4 的改造使得本量化项目的重心从“视觉展现”真正下沉到了“严谨内核”。我们构建起了**“防未来函数 -> 真实券商记账 -> 增量快照缓存 -> 云端持续集成防呆”**的现代化量化回测基座，为将来大规模拓展新资产铺平了道路。
